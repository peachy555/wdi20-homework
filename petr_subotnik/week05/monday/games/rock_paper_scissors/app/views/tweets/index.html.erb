<h1>All da tweetz</h1>

<%= link_to "New Tweet", new_tweet_path %>

<ul>
  <% @tweets.each do |tweet| %>
    <li><%= tweet.message %> | <%= link_to "Edit", edit_tweet_path(tweet) %> | <%= link_to "Delete", tweet_path(tweet), method: :delete %> </li>
  <%end%>
</ul>
